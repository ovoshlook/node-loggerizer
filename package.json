{
  "name": "logger",
  "version": "1.0.0",
  "description": "Really small logger that allows to set layers of logging output such as: ERROR, WARNING, INFO, NOTICE, DEBUG and format it as [formatted-time]: LEVEL: text format",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
     "logger",
	   "logs",
	   "output",
	   "formatted"
  ],
  "author": "Iurii Gorlichenko aka ovoshook",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.2.5"
  }
}
